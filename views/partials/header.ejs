<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Add the link to the CSS stylesheet here -->
  <!-- Hint: the link is relative to the public folder -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

  <link rel="stylesheet" href="styles/main.css">
  <title>To Do List</title>
  
</head>
<body>
  <div class="container">
      <header class="d-flex justify-content-center py-3">
        <ul class="nav nav-pills">
          <li class="nav-item"><a href="/" 
          <% if (activeTab === "index") { %> class="nav-link active" <% } else {  %> class="nav-link" <% } %>aria-current="page">Entry</a></li>
          <li class="nav-item"><a href="/features" 
          <% if (activeTab === "features") { %> class="nav-link active" <% } else {  %> class="nav-link" <% } %>aria-current="page">View</a></li>
        </ul>
      </header>
    </div>